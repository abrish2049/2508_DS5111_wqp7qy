sudo apt update # To bring VM snapshot up to date with package versions
sudo apt install make -y # so we can use makefiles
sudo apt install python-venv -y 
### had issues with the Ubuntu OS i used so i did the generic python build instead 
### so we can create python virtual environments
sudo apt install tree
